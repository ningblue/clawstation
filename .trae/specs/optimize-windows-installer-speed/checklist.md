*- [x] `archiver` 已安装在 `devDependencies` 中
- [x] `scripts/archive-openclaw.js` 存在且能成功创建 zip 文件
- [x] `package.json` 构建脚本包含压缩步骤
- [x] `electron-builder.yml` 引用了 `openclaw.zip` 而不是目录
- [x] `scripts/installer.nsh` 包含有效的 NSIS 解压代码
- [x] 构建过程无错误完成

